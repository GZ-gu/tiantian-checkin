# 天天打卡 1.2.15 视觉验收报告

> 状态：历史视觉基线，仅记录 `1.2.15` 的逐页截图与差异结果。当前发布版本及后续修正以 `../天天打卡-产品开发交付文档.md` 为准。

## 验收范围

- Figma文件：`fms7qDu4GjPsyWlEXG5tnh`
- 正式UI区：`179:1453`
- 页面入口：`106:71`
- 基准视口：`393 x 851`
- 响应式视口：`360 x 800`
- 源图：`source/`
- 浏览器截图：`qa-1.2.15/`
- 差异图：`visual-diff-1.2.15/`

## 页面与代码对应

| 页面/状态 | Figma节点 | 代码元素 |
| --- | --- | --- |
| 首次空首页 | `189:2370` | `.home-screen.is-empty` |
| 有任务首页 | `106:124` | `.home-screen.has-tasks` |
| 展开成长轨迹 | `189:2645` | `.growth-card.expanded` |
| 发布任务 | `147:491` | `.publish-screen` |
| 编辑任务 | `149:933` | `.edit-screen` |
| 我的勋章 | `147:662` | `.badges-screen` |
| 个人信息 | `179:1909` | `.profile-screen` |
| 打卡记录 | `153:451` | `.records-screen` |
| 情况说明 | `156:821` | `.modal-revoke` / `.modal-makeup` |
| 打卡二次确认 | `151:1226` | `.modal-checkin` |
| 打卡成功 | `112:1818` | `.standard-success` |
| 获得勋章 | `179:1460` | `.milestone-success` |
| 目标达成 | `156:991` | `.reward-screen` |
| 分享页 | `172:1800` | `.share-screen` |

## 素材校验

运行时只引用 `assets/figma/` 和 `assets/fonts/`。首页、导航、返回、分享、补卡、撤销、成功状态、勋章环、狮子、植物、二维码和五档字体均来自Figma正式节点或项目指定本地字体包。

未使用页面截图、画板截图或JPG贴图代替可实现UI。`source/*.png`只用于视觉对比，不参与运行时渲染。分享海报由DOM、SVG、PNG和真实文本组合生成，不是整页截图。

## 逐页结果

| 页面/状态 | 393结果 | 360结果 | 动态差异说明 |
| --- | --- | --- | --- |
| 首次空首页 | 几何和素材对齐 | 通过 | 当前日期、时间 |
| 有任务首页 | 几何和素材对齐 | 通过 | 当前日期、任务数据 |
| 展开成长轨迹 | 月历结构对齐 | 通过 | 当前月份、真实任务 |
| 发布任务 | 表单和列表对齐 | 通过 | 默认目标值、任务数据 |
| 编辑任务 | 对齐 | 通过 | 奖励文案 |
| 我的勋章 | 对齐 | 通过 | 已获得数量决定彩色/灰色 |
| 个人信息 | 对齐 | 通过 | 无业务差异 |
| 打卡记录 | 时间轴对齐 | 通过 | 记录数量、日期和原因 |
| 情况说明 | 对齐 | 通过 | 补卡/撤销标题 |
| 打卡二次确认 | 对齐 | 通过 | 任务名称和进度 |
| 打卡成功 | 对齐 | 通过 | 实时统计 |
| 获得勋章 | 对齐 | 通过 | Figma示例进度与真实规则冲突，运行时采用7/7 |
| 目标达成 | 布局对齐 | 通过 | 7/14/21/30天使用对应勋章 |
| 分享页 | 节点坐标对齐 | 通过 | 累计天数和当前任务 |

原始像素差异受实时系统时间、真实日期、动态任务数据、勋章状态和浏览器抗锯齿影响，不作为单一通过条件。每页保留原始差异图；验收同时检查节点坐标、素材来源、computed样式、溢出和真实交互。

## 浏览器验证

- 393视口：14个状态截图均为 `393 x 851`，横向溢出为0，未发现非装饰元素越界。
- 360视口：页面实际内容宽度为360px，长页面隐藏滚动条但保留滚动能力，未发现右侧裁切。
- 字体：等待 `document.fonts.ready` 后截图，computed font-family为 `Alibaba PuHuiTi 2.0`。
- 成功动效：本报告记录的是 `1.2.15` 入场动效；`1.2.18` 已改为原始 SVG 持续循环至离开成功页，`prefers-reduced-motion` 下禁用动画。
- 离线：停止HTTP服务后刷新成功，数据、字体和图片仍可读取。
- 海报：实际下载成功，尺寸 `702 x 1194`，预览外描边未被裁切。
- 线上：GitHub Pages 已部署，393px 首次空首页、版本资源、字体、图片和控制台均通过真实浏览器复核。

## 对抗式复核

- 不把测试夹具带到非 `127.0.0.1` 环境。
- 不用静态示例日期覆盖真实日历规则。
- 不为追求截图相同复制Figma中与产品规则冲突的里程碑数字。
- 不把浏览器截图色彩管理差异改写成新的近似颜色Token。
- 不新增账号、云同步、照片证明、等级或排行榜。
