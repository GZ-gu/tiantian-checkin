# 天天打卡正式产品返工设计

## 决策

保留 `core.mjs` 业务规则层，重写 `app.js` 页面交互和 `styles.css` 视觉层。Figma 正式 UI 区 `179:1453` 的 13 个画板是唯一视觉基准，`docs/天天打卡-产品开发交付文档.md` 是唯一产品与开发依据。

## 用户体验

首次打开显示节点 `189:2370` 的空首页，不展示个人信息拦截页。日期节点可选择并驱动任务区域显示对应日期状态；直接打卡仅限今天，历史日期进入补卡或记录流程。所有可见图标来自 Figma 导出资源。

## 交付

静态离线 PWA 部署到 `GZ-gu/tiantian-checkin` 的 GitHub Pages。使用 393px Figma 原生尺寸做逐页截图比对，360px 做响应式验证，并分别完成干净数据流程和复杂历史数据流程测试。
