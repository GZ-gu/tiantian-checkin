# 天天打卡项目规则

## 项目定位

这是“天天打卡”V1.1 产品规格的 PWA 实现：把家庭学习任务的每日完成转化为可见的勋章成长和奖励回应。当前发布版本以 `README.md` 为准。

## 现役依据

- 产品规则以 `docs/天天打卡_V1.1_PRD.md` 为唯一来源。
- 视觉以 Figma 文件“天天打卡”正式 UI 区的 13 个主流程画板和首次空首页为准。
- `docs/archive/` 和 Figma 旧画板是历史参考，不得覆盖 V1.1 规则。
- `index.html`、`styles.css`、`app.js` 是正式产品入口，`core.mjs` 是规则层。

## 当前状态

- 已实现：首次空状态、日期切换、直接打卡、过去日期补卡、原因必填的撤销、记录追溯、勋章回退、最终目标奖励归档、分享海报和离线 PWA。
- 打卡成功页使用 `assets/figma/confetti.svg` 持续循环，离开成功页后停止；减少动效模式不播放。
- 测试夹具仅允许在 `127.0.0.1` 使用，正式部署不得携带测试状态。
- 数据只保存在当前浏览器设备，清理浏览器数据或卸载 PWA 会删除记录。

## 运行与验证

- 必须通过 HTTP 或 HTTPS 运行，不能把直接打开 `index.html` 作为 PWA 验收方式。
- 每次功能变更需同时核对 README、V1.1 PRD 和交付文档。
- 发布前运行规则测试，并完成 393px、360px 和离线浏览器验收。

## 开发约定

- 不引入账号、云同步、照片证明、成长等级或排行榜。
- 运行时视觉只使用 `assets/figma/` 和 `assets/fonts/`，禁止用字符或自造图标替代 Figma 素材。
- 不删除、移动或重命名根目录素材，除非用户确认过清理候选清单。
